spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_hotel_test?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate

aliyun:
  dashscope:
    api-key: ${DASHSCOPE_API_KEY}
    model: qwen-turbo
    timeout: 30000

logging:
  level:
    root: WARN
    com.ai.hotel: INFO
